# Core scientific stack for the superspreader SIR reimplementation.
# Python 3.10-3.13 supported (Numba >= 0.61 is required for Python 3.13).
numpy>=1.26
numba>=0.61
scipy>=1.11
matplotlib>=3.8
joblib>=1.3
tqdm>=4.66

# Optional GPU backend (Tier-3, only needed for very large N / huge sweeps):
# cupy-cuda12x>=13.0
